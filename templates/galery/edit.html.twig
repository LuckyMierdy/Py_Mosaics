{% extends 'base.html.twig' %}

{% block title %}Éditer une galerie
{% endblock %}

{% block body %}
	<h2 class="text-center mt-5">Éditer une galerie</h2>
	<div class="d-flex flex-column align-items-center">

		{{ include('galery/_form.html.twig', {'button_label': 'Mise à jour'}) }}

		<a href="{{ path('app_galery_index') }}">Retour à la liste de galerie</a>

		{{ include('galery/_delete_form.html.twig') }}
	</div>
{% endblock %}
